{
  "name": "listedb-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=@listedb/core && npm run build --workspace=@listedb/cli",
    "generate": "npm run generate --workspace=@listedb/cli"
  },
  "devDependencies": {
    "typescript": "^5.5.2"
  }
}
